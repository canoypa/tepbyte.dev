---
import type { HTMLTag, Polymorphic } from 'astro/types'
import { css } from '~pandacss/css'

const styles = {
  root: css({
    display: 'inline-flex',
    alignItems: 'center',
    paddingX: 'md.space200',
    height: 32,
    borderWidth: 1,
    borderStyle: 'solid',
    borderColor: 'md.outline-variant',
    color: 'md.on-surface',
    rounded: 'md.small',
    textStyle: 'md.label-large',
    fontFamily: 'comfortaa',
    cursor: 'pointer',
    transitionProperty: 'background-color',
    transitionDuration: 'md.medium1',

    backgroundColor: {
      _hover: 'md.on-surface/md.hover',
      _focusVisible: 'md.on-surface/md.focus',
      _active: 'md.on-surface/md.pressed',
    },
  }),
}

type Props<T extends HTMLTag> = Polymorphic<{
  as: T
}>

const { as: Tag = 'span', ...attrs } = Astro.props
---

<Tag {...attrs} class:list={styles.root}>
  <slot />
</Tag>
