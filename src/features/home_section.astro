---
import Button from '~/components/button/button.astro'
import { css } from '~pandacss/css'
import { flex } from '~pandacss/patterns'

type Props = {
  title: string
  moreHref: string
}

const { title, moreHref } = Astro.props

const hasActionsTrailing = Astro.slots.has('actions-trailing')

const styles = {
  root: flex({ direction: 'column', rowGap: 32 }),
  title: css({
    textStyle: 'display-small',
    fontFamily: 'comfortaa',
    fontWeight: 'bold',
  }),
  actions: flex({
    direction: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    columnGap: 24,
  }),
}
---

<section class:list={styles.root}>
  <h2 class:list={styles.title}>{title}</h2>
  <slot />
  <div class:list={hasActionsTrailing ? styles.actions : undefined}>
    <Button as="a" href={moreHref}>すべて見る</Button>
    {hasActionsTrailing && <slot name="actions-trailing" />}
  </div>
</section>
