name: CI

on:
  pull_request:

# 同じブランチへの連続 push では最新のみ検証する
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/build.yaml
